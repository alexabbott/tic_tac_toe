// var app = angular.module('toeApp', []);

// app.controller('gameController', function($scope) {
// 	$scope.values = [1,2,3,4,5,6,7,8,9];

// 	// instantiate empty arrays for playerX and playerO
// 	$scope.playerX = [];
// 	$scope.playerO = [];

// 	$scope.boxClicked = function(index) {
// 		if (($scope.playerX + $scope.playerO) == 0 || ($scope.playerX + $scope.playerO) == 2 || ($scope.playerX + $scope.playerO) == 4 || ($scope.playerX + $scope.playerO) == 6) {
// 			$scope.playerX.push($scope.values[index]);
// 		}
// 	};

// });




